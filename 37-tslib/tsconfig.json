{
  "compilerOptions": {
    "target": "es5",
    "lib": ["es6", "dom"],
    "module": "commonjs",
    "outDir": "dist",

    // Reduces emitted file size by relying on functions such as __extends
    // "noEmitHelpers": true,

    // Import helper functions such as __extends from tslib (tslib must be in package)
    // Better version of noEmitHelpers because it ensures that the helper functions exist
    "importHelpers": true
  }
}
